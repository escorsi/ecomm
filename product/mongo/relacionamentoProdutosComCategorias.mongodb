use('ecomm-product');

db.categories.find({"STATUS": "ATIVA"}).forEach((category) => {
    db.products.updateMany({
        "CATEGORIA": { $eq: category.NOME }
    }, {
        $set: {
            'categoria.categoria_id': category._id,
            'categoria.nome': category.NOME
        }
    })
});



